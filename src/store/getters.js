export const tabSelected = state => {
  return state.tab.tabSelected
}
export const tabContainerActive = state => {
  return state.tab.tabContainerActive
}

export const logined = state => {
  return state.account.logined
}

export const loginedUserId = state => {
  return state.account.userId
}
